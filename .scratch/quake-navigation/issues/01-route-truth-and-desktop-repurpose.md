# Route truth and Desktop repurpose

Status: completed
Blocked by: none

## Scope

Implement the route and content decisions from `.scratch/quake-navigation/spec.md` §§115–126 without changing primary navigation markup or interaction code.

- Add `/new-releases/` and `/accessories/` using established content-route front matter and honest empty states.
- Repurpose `/desktop/` to aspirational desktop hardware. No label, metadata, card, footer, or contextual link may describe that URL as the current Omarchy desktop environment.
- Update product/status data and all affected Platform, Hardware, Mobile, Cloud, Server, and footer references coherently. Preserve released OS ISO and Manual actions outside this route.
- Resolve the coupled Desktop proof-media ownership intentionally; do not leave an obsolete required proof assertion or break deterministic asset checks.
- Register new content directories in the existing content and CSS source scanners.

## Acceptance

The three routes render with accurate metadata and copy. `/desktop/` is unambiguously exploratory hardware; `/accessories/` makes no unsupported recommendation; `/new-releases/` makes no release promise. Existing OS guidance remains reachable. Do not edit navigation/header/search modules or production Chromium verifier.
